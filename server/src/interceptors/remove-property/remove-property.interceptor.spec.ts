import { RemovePropertyInterceptor } from './remove-property.interceptor';

describe('RemovePropertyInterceptor', () => {
  it('should be defined', () => {
    expect(new RemovePropertyInterceptor()).toBeDefined();
  });
});
